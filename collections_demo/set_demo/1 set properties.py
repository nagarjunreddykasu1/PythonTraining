"""
set:
    set is a collection datatype which holds group of values

1. set is represented using {}
2. set supports both homogeneous (similar type) and heterogeneous (different type) elements
x={10,"python", 15.75, True}

3. set does not allow duplicate elements
x={10,20,20,10,30,40}

4. In set, insertion order is NOT preserved.

5. Each element of the set is NOT accessed by a unique index
we can access the elements using for loop.

6. Set is a mutable object. modifications are allowed, But, elements of set should be immutable only.

7. set can also be created using set() function

8. set within a set is not allowed because set allows only immutable objects.


"""